   正则表达式就是处理字符串的方法，它是以行为单位来进行字符串的处理行为，正则表达式通过一些特殊符号的辅助，
可以让用户轻易达到查找，删除，替换某特定字符串的处理程序。

基础正则表达式字符^word待查找的字符串（word）在行首 word$待查找的字符串（word）在行尾 . 代表一定有一个任意字符的字符  e.e -> 可以是eee， eae，e e等等
\ 转义字符 将特殊符号的特殊意义去除 
* 重复0到无数个的前一个字符

[list]  a[afl]b 就是 aab afb alb的意思 
[n1-n2] 找到想要的字符范围 [0-9]

[^list] 从字符集合的RE字符里面找出不要的字符串或范围          
\{n,m\} 连续n到m个的前一个RE字符，若为\{n\}则是连续n个的前一个RE字符，若为\{n,\}则是连续n个以上的前一个RE字符 go\{2,3\}gle 中间有两个到三个o
